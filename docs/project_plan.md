# Galaxy Typing 프로젝트 계획

## 📋 프로젝트 개요
- **프로젝트명**: Galaxy Typing
- **설명**: 우주 테마의 Flutter 타이핑 연습 앱
- **플랫폼**: 모바일 (iOS/Android), 웹 (Desktop/Tablet)
- **개발 언어**: Dart/Flutter

## 🎯 주요 목표
1. **생동감 있는 UI/UX**: 우주 테마의 인터랙티브한 사용자 경험
2. **플랫폼별 최적화**: 모바일과 웹 환경의 차이점을 고려한 구현
3. **정확한 타이핑 측정**: 한글 조합 과정을 고려한 정밀한 CPM/정확도 계산
4. **몰입감 있는 경험**: 사운드, 애니메이션, 피드백의 조화

## 📅 작업 진행 상황

### ✅ 완료된 작업

#### 2025.06.18 16:45:32 - 다크/라이트 모드 제거 및 단일 우주 테마 적용
**문제 해결**: 버튼과 글자 색상이 같아서 안보이는 문제 해결
**변경 내용**:
1. **AppTheme 완전 개편**:
   - 다크/라이트 모드 관련 함수 제거 (darkTheme, lightTheme)
   - 단일 cosmicTheme() 함수로 통합
   - 색상 대비 개선으로 가독성 향상
   - 버튼 전용 색상 시스템 도입 (buttonPrimaryBg, buttonPrimaryText 등)

2. **SettingsController 수정**:
   - darkThemeEnabled 변수 및 관련 로직 완전 제거
   - themeMode getter를 단일 다크 테마로 고정
   - 저장/로드 로직에서 다크 모드 키 제거

3. **CosmicButton 색상 시스템 개선**:
   - 새로운 색상 체계로 버튼 타입별 색상 분리
   - 텍스트와 배경색 대비 문제 해결
   - Primary, Secondary, Outline 버튼별 최적화된 색상

4. **설정 화면 완전 재작성**:
   - 테마 설정 섹션 완전 제거
   - 테마 관련 함수들(_buildThemeSelector, _buildThemeOption) 삭제
   - 단일 우주 테마에 맞는 UI 구성

5. **main.dart 테마 적용 수정**:
   - theme과 darkTheme 중복 제거
   - 단일 cosmicTheme() 적용

**결과**: 색상 대비 문제 해결, 일관된 우주 테마 경험 제공

#### 2025.06.18 13:51:38 - 테두리 색상 가시성 및 조화 개선
**문제 해결**: 버튼과 텍스트 컨테이너의 테두리 색상 조정 (너무 진함 → 너무 어두움 → 적절한 중간 톤)
**변경 과정**:
1. **1차 조정 (13:49:31)**:
   - 기존: `Color(0xFF00D4FF)` (너무 밝은 시안색) → `Color(0xFF1A2332)` (너무 어두워서 거의 안 보임)

2. **2차 조정 (13:51:38) - 최종**:
   - **borderColor**: `Color(0xFF3A4A5C)` (적당히 보이는 어두운 푸른 회색)
   - **borderColorHover**: `Color(0xFF4A5A6C)` (호버 시 더 밝게)
   - **buttonOutlineBorder**: `Color(0xFF4A5A6C)` (적당히 보이는 테두리)

3. **최종 효과**:
   - 테두리가 적절히 보이면서도 배경과 조화
   - 과도하지 않은 대비로 시각적 편안함 제공
   - 우주 테마의 세련된 외관 유지

**결과**: 가시성과 조화의 완벽한 균형을 이룬 테두리 색상 완성

#### 2025.06.13 15:27:02 - 한글 조합 중 오타 평가 제외 개선
**개선 배경**: 
- 사용자 피드백: "조합 중엔 오타 평가를 안해야 맞는거 아냐?"
- 기존 방식의 문제: 조합 중인 문자에도 부분 점수를 주고 오타 카운트 진행

**개선 내용**:
1. **조합 중 문자 평가 제외**:
   - `_isInComposition()` 함수로 조합 상태 정확히 판별
   - 조합 중인 문자는 정답도 오타도 아닌 "평가 제외" 상태로 처리
   - 자모와 완성형 혼재, 초성/중성/종성 조합 과정 모두 감지

2. **정확도 계산 개선**:
   - 실제 평가된 문자 수(`evaluated`)만을 분모로 사용
   - 조합 중인 문자는 계산에서 완전히 제외
   - 더 정확하고 공정한 정확도 측정

3. **CPM 계산 단순화**:
   - 완성된 문자 + 평가 가능한 문자만으로 계산
   - 조합 제외로 더 안정적인 스무딩 (0.8~0.9 팩터)
   - 예측 기반 계산 제거로 더 정확한 측정

**결과**: 한글 조합 특성을 완벽 반영한 자연스럽고 정확한 타이핑 측정 시스템

#### 2025.06.13 14:45:32 - 웹 마우스 인터랙션 시스템 구현
**구현 내용**:
1. **MouseInteractionHandler 위젯 생성**:
   - 마우스 트레일 시스템 (TrailParticle)
   - 클릭 파티클 폭발 효과 (ClickParticle)
   - 커스텀 페인터로 파티클 렌더링
   - 플랫폼 감지로 웹에서만 활성화

2. **호버 효과 시스템**:
   - HoverEffectWrapper로 확대/그림자 효과
   - 게임 모드 카드: 1.05배 확대
   - 통계 카드: 1.02배 확대  
   - 하단 버튼: 1.1배 확대

3. **커서 커스터마이징**:
   - CustomCursorType enum (normal, hover, click)
   - 상태별 커서 변화 및 동적 글로우 효과

#### 2025.06.13 14:20:15 - 플랫폼별 구현 전략 문서화
**enhancement_ideas.md 대폭 개선**:
1. **플랫폼별 전략 수립**:
   - 모바일: 성능 최적화, 터치 특화, 햅틱 피드백
   - 웹: 풍부한 효과, 마우스 활용, 고품질 오디오

2. **세부 구현 방안**:
   - 애니메이션 효과 차별화
   - 인터랙티브 배경 전략
   - 타이핑 시각 효과 플랫폼별 최적화
   - 사운드 전략 수립

3. **개발 가이드라인**:
   - 플랫폼 감지 코드 예시
   - 성능 모니터링 시스템
   - 구현 우선순위 3단계 분류

#### 2025.06.13 13:50:42 - 오디오 서비스 최적화
**문제 해결**: 모바일에서 효과음 재생 시 다른 앱 오디오 중지 문제
**해결 방안**: 
- 단일 AudioPlayer 인스턴스 사용으로 오디오 포커스 경쟁 해결
- 사운드 파일 사전 로딩으로 지연 시간 최소화
- 플랫폼별 오디오 설정 최적화

#### 2025.06.13 13:30:18 - 게임 완료 후 결과 유지 시스템
**구현 내용**:
- 최종 통계 저장 변수 추가 (_finalCpm, _finalAccuracy 등)
- 게임 완료 시 결과 표시 및 유지
- 새 게임 시작 시스템으로 이전 결과 보존
- 통계 박스에서 최종 결과 지속 표시

#### 2025.06.13 12:45:27 - 한글 입력 처리 시스템 개선
**구현 내용**:
- 한글 조합 상태 고려 로직 추가
- 유니코드 안전 문자 계산 (이모지 대응)
- CPM 계산 스무딩 효과 적용
- 게임 완료 후 입력 차단 시스템

#### 2025.06.13 12:15:33 - WPM → CPM 통일 및 마우스 커서 개선
**변경 사항**:
- long_text_practice_screen.dart에서 WPM → CPM 변경
- 계산 방식 통일: `문자수/분`
- 마우스 커서 상태별 변화 구현

#### 2025.06.13 19:32:39 - 자소 단위 타수 계산 시스템 구현
**구현 내용**:
1. **자소 단위 타수 계산 시스템 구현**:
   - 한글: 초성+중성+종성 자소 단위 계산 ("안녕" = 6타)
   - 영어: 글자 단위 계산 ("Hello" = 5타)
   - 정확한 타수 공식 적용:
     - 타수 = (입력한 자소수 × 60) / 걸린시간(초)
     - 정타수 = (올바른 자소수 × 60) / 걸린시간(초)
     - 정확도 = (올바른 자소수 / 전체 입력 자소수) × 100
     - 실타수 = 정타수 × (정확도 / 100)

2. **한글 분해 및 비교 시스템**:
   - 완성형 한글 자소 분해 함수 구현
   - 자소 단위 텍스트 비교 로직
   - 한글/영어 혼합 텍스트 처리

3. **UI 표시 개선**:
   - CPM → "타수"로 표시 변경
   - 통계 박스에서 자소 단위 계산 반영
   - 결과 화면에서 자소 수 표시

4. **적용 범위**:
   - 긴글 연습 화면 (long_text_practice_screen.dart)
   - 기본 연습 화면 (basic_practice_screen.dart)
   - 모든 타수 계산 로직 통일

### 🔄 진행 중인 작업
- 없음

### 📋 예정된 작업

#### 1단계: 핵심 기능 완성 (우선순위: 높음)
- [ ] **타이핑 게임 로직 완성**
  - [ ] 단어 연습 모드 구현
  - [ ] 문장 연습 모드 구현  
  - [ ] 긴글 연습 모드 구현
  - [ ] 실시간 통계 계산 (CPM, 정확도)

- [ ] **기본 UI/UX 완성**
  - [ ] 메인 화면 디자인
  - [ ] 게임 화면 레이아웃
  - [ ] 결과 화면 구성
  - [ ] 설정 화면 구현

#### 2단계: 플랫폼별 최적화 (우선순위: 중간)
- [ ] **모바일 최적화**
  - [ ] 터치 인터페이스 개선
  - [ ] 햅틱 피드백 구현
  - [ ] 세로/가로 모드 대응
  - [ ] 가상 키보드 최적화

- [ ] **웹 최적화**  
  - [x] 마우스 인터랙션 구현
  - [ ] 키보드 단축키 지원
  - [ ] 반응형 디자인 완성
  - [ ] 브라우저 호환성 테스트

#### 3단계: 고급 기능 추가 (우선순위: 낮음)
- [ ] **시각 효과 강화**
  - [ ] 파티클 시스템 구현
  - [ ] 타이핑 애니메이션 효과
  - [ ] 배경 인터랙션 구현
  - [ ] 테마 시스템 구축

- [ ] **사운드 시스템**
  - [x] 기본 효과음 구현
  - [ ] 배경음악 시스템
  - [ ] 사운드 설정 옵션
  - [ ] 플랫폼별 오디오 최적화

- [ ] **데이터 관리**
  - [ ] 로컬 저장소 구현
  - [ ] 통계 데이터 관리
  - [ ] 설정 데이터 관리
  - [ ] 데이터 백업/복원

## 🛠 기술 스택
- **Frontend**: Flutter (Dart)
- **상태 관리**: Provider
- **로컬 저장소**: SharedPreferences
- **오디오**: audioplayers
- **플랫폼**: iOS, Android, Web

## 📝 개발 노트
- 플랫폼별 차이점을 고려한 조건부 구현 필요
- 성능 최적화를 위한 플랫폼별 설정 분리
- 한글 입력 특성을 고려한 정확도 계산 로직 필수
- 사용자 경험 향상을 위한 부드러운 애니메이션과 피드백 중요

## 🎯 다음 단계
1. **타이핑 게임 로직 완성**: 기본적인 게임 플레이 기능 구현
2. **플랫폼별 최적화**: 모바일과 웹 환경에 맞는 UI/UX 개선  
3. **시각/청각 효과 추가**: 몰입감 있는 사용자 경험 구현
4. **성능 최적화 및 테스트**: 안정성과 성능 확보

## 🎯 다음 작업 계획
1. **다른 연습 모드에 자소 단위 적용**:
   - 단어 연습 모드
   - 문장 연습 모드
   - 기타 연습 모드들

2. **성능 최적화**:
   - 자소 분해 함수 캐싱
   - 대용량 텍스트 처리 최적화

3. **사용자 경험 개선**:
   - 자소 단위 계산 설명 추가
   - 타수 계산 방식 선택 옵션

## 🎯 다음 마일스톤
- **다음 마일스톤**: 모든 연습 모드에 자소 단위 적용
- **전체 진행률**: 약 85% 완료

## 🎯 기술적 세부사항

### 자소 단위 계산 예시
```
"컴퓨터" 입력 시:
- 컴: ㅋ + ㅓ + ㅁ = 3타
- 퓨: ㅍ + ㅠ = 2타  
- 터: ㅌ + ㅓ = 2타
- 총 7타

10초에 입력 시:
- 타수 = 7 × 60 / 10 = 42타/분
```

### 구현된 핵심 함수들
- `_getJasoCount()`: 텍스트의 총 자소 수 계산
- `_getKoreanJasoCount()`: 한글 문자의 자소 수 계산
- `_decomposeKorean()`: 한글 분해 (초성, 중성, 종성)
- `_compareTextByJaso()`: 자소 단위 텍스트 비교
- `_decomposeTextToJasos()`: 텍스트를 자소 리스트로 분해

## 🎯 이전 작업 이력

### 2025.06.13 이전 작업들
1. **플랫폼별 구현 전략 문서화**:
   - 모바일/웹 환경별 최적화 방안
   - 성능 및 UX 고려사항

2. **웹 마우스 인터랙션 구현**:
   - 마우스 트레일 시스템
   - 클릭 파티클 효과
   - 호버 효과 및 커서 커스터마이징

3. **한글 조합 처리 개선**:
   - 조합 중 문자 처리 로직

#### 2025.06.18 14:05:12 - 복잡한 화면 레이아웃 간소화
**문제 해결**: 통계, 텍스트 업로드, 공유 텍스트 라이브러리 화면이 너무 복잡해진 문제
**변경 내용**:
1. **StatsScreen 간소화**:
   - 복잡한 대시보드 레이아웃 → 간단한 카드 기반 레이아웃
   - 불필요한 차트와 복잡한 통계 제거
   - 메인 화면 스타일과 일관성 있는 디자인 적용
   - 실제 StatsController 속성에 맞게 수정 (totalCharTyped, totalTimeSpent, highestScore 등)

2. **TextUploadScreen 간소화**:
   - 복잡한 마법사 스타일 → 간단한 폼 기반 레이아웃
   - 단계별 프로세스 제거하고 한 페이지에 모든 입력 필드 배치
   - CosmicInputField 파라미터 오류 수정 (labelText → label)
   - 멀티라인 텍스트 입력을 위한 커스텀 TextField 구현
   - 업로드 기능 임시 처리로 간소화

3. **SharedTextLibraryScreen 간소화**:
   - 복잡한 그리드 레이아웃 → 간단한 리스트 기반 레이아웃
   - 과도한 필터 옵션과 정렬 기능 제거 (언어 필터, 복잡한 정렬 제거)
   - 광고 시스템과 복잡한 통계 제거
   - 검색과 기본 필터(카테고리, 난이도)만 유지
   - 텍스트 상세 다이얼로그 제거하고 간단한 리스트 아이템으로 통일

**결과**: 모든 화면이 메인 화면과 일관된 간단하고 직관적인 스타일로 통일됨. 복잡한 기능들을 제거하여 사용자가 핵심 기능에 집중할 수 있도록 개선.

#### 2025.06.18 14:07:25 - UI/UX 세부 개선
**문제 해결**: 공유 라이브러리와 텍스트 업로드 화면의 UI/UX 문제들
**변경 내용**:
1. **공유 텍스트 라이브러리 화면 개선**:
   - 사라진 업로드 버튼을 헤더에 복원 (데스크탑: 텍스트 포함, 모바일: 아이콘만)
   - 불필요한 텍스트 목록 카드 래퍼 제거하고 간단한 헤더로 변경
   - 데스크탑에서 초기화 버튼 위치 조정 (crossAxisAlignment.end 적용)

2. **텍스트 업로드 화면 개선**:
   - 업로드 성공 시 자동으로 이전 화면으로 돌아가도록 수정
   - 업로드 결과를 공유 라이브러리에서 감지할 수 있도록 결과값 반환

3. **전체적 개선 효과**:
   - 더 직관적인 네비게이션 플로우
   - 불필요한 UI 요소 제거로 깔끔한 디자인
   - 데스크탑과 모바일 환경 모두 고려한 반응형 디자인

**결과**: 사용자 경험이 향상되고 더욱 직관적인 인터페이스 완성
   - 이모지 및 유니코드 안전 처리

4. **게임 완료 후 결과 유지 시스템**:
   - 최종 통계 표시
   - 새 게임 시작 기능

5. **오디오 서비스 최적화**:
   - 단일 AudioPlayer 사용
   - 모바일 오디오 포커스 문제 해결

## 🎯 프로젝트 상태
- **현재 상태**: 자소 단위 타수 계산 시스템 완료
- **다음 마일스톤**: 모든 연습 모드에 자소 단위 적용
- **전체 진행률**: 약 85% 완료 

#### 2025.06.18 11:43:03 - 통계 화면과 텍스트 업로드 화면 완전 재설계
**재설계 배경**: 
- 기존 레이아웃의 한계점 인식
- 더 현대적이고 직관적인 사용자 경험 필요
- 플랫폼별 최적화된 새로운 접근 방식 도입

**통계 화면 혁신**:
1. **현대적 대시보드 스타일 적용**:
   - KPI 카드 그리드 시스템 (2x2, 모바일 1x4)
   - 실시간 성능 차트 위젯
   - 최근 활동 타임라인 표시
   - 모드별 상세 통계 섹션

2. **인터랙티브 기능 강화**:
   - 성취 시스템 배지 표시
   - 퀵 인사이트 카드 (개인화된 팁)
   - 목표 설정 및 진행률 추적
   - 실시간 순위 표시

3. **반응형 레이아웃**:
   - 데스크톱: 7:3 비율 (메인 대시보드 + 사이드바)
   - 모바일: 세로 스크롤 최적화
   - 사이드바: 광고 + 퀵 통계 + 인사이트

**텍스트 업로드 화면 혁신**:
1. **마법사 스타일 단계별 프로세스**:
   - 4단계 업로드 프로세스 (기본정보 → 분류 → 콘텐츠 → 검토)
   - 시각적 진행 표시기 (원형 스텝 + 진행바)
   - 단계별 애니메이션 전환 효과

2. **향상된 사용자 경험**:
   - 애니메이션 텍스트 필드 (슬라이드 + 페이드)
   - 실시간 폼 검증 시스템
   - 단계별 완료 상태 표시
   - 검토 단계에서 전체 정보 확인

3. **지능형 가이드 시스템**:
   - 업로드 팁 사이드바
   - 실시간 업로드 통계 표시
   - 저작권 경고 시스템
   - 단계별 도움말 제공

**공통 개선사항**:
1. **프리미엄 광고 시스템**:
   - 데스크톱: 사이드바 다중 광고 슬롯
   - 모바일: 배너 및 네이티브 광고
   - 그라데이션 및 섀도우 효과 적용
   - 광고 크기 및 위치 최적화

2. **애니메이션 시스템**:
   - FadeTransition으로 화면 진입 효과
   - SlideTransition으로 단계 전환
   - TweenAnimationBuilder로 요소별 애니메이션
   - 부드러운 상태 변화 효과

3. **플랫폼 최적화**:
   - ResponsiveHelper 활용한 적응형 레이아웃
   - 데스크톱/태블릿/모바일별 최적화
   - 터치 및 마우스 인터랙션 고려
   - 성능 최적화된 위젯 구성

**결과**: 두 화면 모두 완전히 새로운 차원의 사용자 경험 제공, 현대적 디자인 패턴 적용, 플랫폼별 최적화 완료

#### 2025.06.18 14:09:32 - 버튼 클릭 문제 해결 완료
**문제 발견**: 공유 텍스트 라이브러리 화면에서 모든 버튼들이 클릭되지 않는 문제
**원인 분석**: main.dart에서 필수 라우트 설정이 누락됨
**해결 과정**:
1. **라우트 설정 추가**:
   - `/text-upload`: TextUploadScreen 라우트
   - `/long-text-practice`: LongTextPracticeScreen 라우트  
   - `/shared-text-library`: SharedTextLibraryScreen 라우트
   - 메인 라우트 `/` 설정

2. **필수 Import 추가**:
   - `views/text_upload_screen.dart`
   - `views/long_text_practice_screen.dart`
   - `views/shared_text_library_screen.dart`

3. **디버깅 시스템 구축**:
   - 모든 버튼 클릭 이벤트에 디버그 로그 추가
   - 데이터 로딩 상태 추적 로그 추가
   - 네비게이션 오류 처리 및 사용자 피드백 추가

4. **안정성 개선**:
   - `mounted` 체크 추가로 메모리 누수 방지
   - try-catch 블록으로 예외 처리 강화
   - 사용자 피드백을 위한 SnackBar 메시지 추가

**해결 결과**:
- 모든 버튼이 정상적으로 클릭되고 액션 수행
- 화면 간 네비게이션이 원활하게 작동
- 오류 발생 시 적절한 사용자 피드백 제공
- 앱 안정성 크게 향상

**기술적 세부사항**:
- MaterialApp의 routes 속성을 통한 명시적 라우트 등록
- Navigator.pushNamed()를 통한 타입 안전한 네비게이션
- 각 화면별 독립적인 라우트 관리로 확장성 확보

#### 2025.06.18 14:11:54 - 라우트 설정 충돌 문제 해결
**문제 발견**: MaterialApp에서 `home`과 `routes['/']`를 동시에 설정하여 충돌 발생
**오류 메시지**: "If the home property is specified, the routes table cannot include an entry for \"/\""
**해결 방법**:
- `home` 속성 제거
- `initialRoute: '/'` 추가하여 초기 라우트 명시적 설정
- routes 테이블만 사용하여 모든 라우트 관리 통일

**결과**: 라우트 설정 충돌 해결, 앱이 정상적으로 시작되고 모든 네비게이션 기능 정상 작동

#### 2025.06.18 14:15:10 - 긴글 연습 텍스트 선택 모달 추가
**개선 요청**: 긴글 연습에서 랜덤 텍스트 선택 대신 사용자가 직접 텍스트를 선택할 수 있도록 개선
**구현 내용**:
1. **텍스트 선택 모달 추가**:
   - 앱 시작 시 자동으로 텍스트 선택 모달 표시
   - 한국어/영어 설정에 따라 해당 언어의 텍스트 목록 표시
   - 각 텍스트별 제목, 미리보기, 자소 수, 문장 수 정보 제공

2. **사용자 경험 개선**:
   - 텍스트 카드 형태로 직관적인 선택 인터페이스
   - 텍스트 미리보기로 내용 파악 가능
   - 자소 수와 문장 수 표시로 난이도 예상 가능
   - "랜덤 선택" 옵션도 유지하여 기존 기능 보존

3. **모달 디자인**:
   - 우주 테마에 맞는 다크 디자인
   - 스크롤 가능한 텍스트 목록
   - 선택 시 즉시 게임 준비 상태로 전환
   - barrierDismissible: false로 반드시 선택하도록 설정

4. **기능 통합**:
   - "새 텍스트" 버튼도 모달 표시로 변경
   - 텍스트 미선택 시 안내 메시지 표시
   - 커스텀 텍스트(공유 텍스트) 사용 시에는 모달 표시하지 않음

**결과**: 사용자가 원하는 텍스트를 직접 선택할 수 있어 연습 효율성과 만족도 크게 향상

#### 2025.06.18 14:20:09 - 텍스트 선택 모달 반응형 크기 조정 완료
**문제 발견**: 웹에서 텍스트 선택 모달이 너무 크게 표시되어 사용자 경험 저하
**해결 과정**:
1. **AlertDialog → Dialog 변경**: 더 정확한 크기 제어를 위해 Dialog 위젯으로 변경
2. **반응형 크기 설정**:
   - 데스크톱: 최대 너비 600px, 높이 500px
   - 태블릿: 최대 너비 500px, 높이 450px  
   - 모바일: 최대 너비 400px, 높이 400px
3. **컨텐츠 최적화**:
   - 데스크톱에서 더 많은 텍스트 미리보기 (120자 vs 80자)
   - 플랫폼별 폰트 크기 조정 (데스크톱에서 약간 더 크게)
   - 패딩과 마진 최적화
4. **레이아웃 개선**:
   - 헤더, 컨텐츠, 하단 버튼 영역 명확히 구분
   - 스크롤 영역과 고정 영역 분리
   - 버튼 배치 최적화

**결과**: 모든 플랫폼에서 적절한 크기의 모달 표시, 웹에서도 과도하게 크지 않은 사용자 친화적 인터페이스 완성

#### 2025.06.18 14:25:17 - 긴글 연습 완료 후 통계 유지 문제 해결
**문제 발견**: 긴글 연습 완료 후 통계 영역의 최종 통계가 사라지는 문제
**원인 분석**: 
1. `_calculateFinalStats()` 메서드에서 최종 통계를 별도 변수에 저장하지 않음
2. `_startNewGame()` 및 텍스트 선택 시 최종 통계 변수들이 초기화됨

**해결 과정**:
1. **최종 통계 저장 개선**:
   - `_calculateFinalStats()` 메서드에서 `_finalCpm`, `_finalAccuracy`, `_finalElapsedTime`, `_finalCompletedSentences` 변수에 최종 값 저장
   - 게임 완료 시점에 확실히 최종 통계 저장

2. **새로운 리셋 메서드 추가**:
   - `_resetGameKeepFinalStats()` 메서드 추가
   - 게임 상태는 초기화하되 최종 통계 변수들은 유지
   - 텍스트 선택 시 이 메서드 사용

3. **게임 플로우 개선**:
   - `_selectRandomText()`, `_selectSpecificText()` 메서드에서 최종 통계 유지 리셋 사용
   - `_startNewGame()` 메서드를 텍스트 선택 모달 표시로 단순화
   - 새 게임 시작 시에도 이전 게임의 최종 통계 유지

4. **통계 표시 로직 개선**:
   - 게임 완료 상태에서는 항상 최종 통계 변수 사용
   - 게임 진행 중에는 실시간 통계 변수 사용
   - 통계 박스에서 조건부 표시 로직 정확히 구현

**결과**: 
- 긴글 연습 완료 후 통계 영역에 최종 결과가 계속 표시됨
- "다시 시작" 버튼 클릭 후에도 이전 게임의 최종 통계 확인 가능
- 새 텍스트 선택 시에도 이전 결과 유지되어 비교 가능
- 사용자가 자신의 성과를 지속적으로 확인할 수 있는 개선된 UX 제공 